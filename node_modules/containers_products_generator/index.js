const { object } = require('joi');
const Product= require('./Container')

const p1 = new Product();
//p1.save({name:"Tacos",description:"this are a mexican dish"})

async function show_value(){
    const object1=await p1.getAll()
    console.log(object1[0].id)
    const objecfound=await p1.getById(object1[0].id)
    console.log(objecfound)
    const updatedId=await p1.updateById(object1[0].id,"Maricos","This a different description","now i have a link")
    console.log(updatedId)
    console.log( await p1.getAll())
}
async function show_valueId(){
    console.log(await p1.getById(object1[0].id))
}
show_value()
//p1.deleteAll()